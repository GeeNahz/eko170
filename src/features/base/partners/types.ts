export type PartnerStat = {
  id: string;
  value: string;
  label: string;
};

export type HeadlinePartner = {
  id: string;
  tierLabel: string;
  logoSrc: string;
  logoAlt: string;
  description: string;
};

export type WhyPartnerCard = {
  id: string;
  title: string;
  description: string;
};
