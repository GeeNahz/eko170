export type RouteStat = {
  id: string;
  label: string;
  value: string;
};

export type FigureStat = {
  id: string;
  value: string;
  label: string;
};

export type SponsorLogo = {
  id: string;
  name: string;
  src: string;
};

export type GalleryPhoto = {
  id: string;
  src: string;
  alt: string;
};
